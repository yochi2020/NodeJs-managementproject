<style type="text/css">
html, body { 
   height: 100%; /* ให้ html และ body สูงเต็มจอภาพไว้ก่อน */
   margin: 0;
   padding: 0;
}
   .footer {
  position: fixed;
  bottom: 0;
  width: 100%;
  /* Set the fixed height of the footer here */
  height: 60px;
  line-height: 60px; /* Vertically center the text there */
  background-color: #f5f5f5;
}
    </style>
<footer class="text-center footer" > 
   <% if(locals.user){ %> 
      <a href="/user/logout" class="text-muted" >Logout</a>

   <% }else{ %> 
      <a href="/user/login" class="text-muted">backend</a> 
   <% } %> 
</footer>
